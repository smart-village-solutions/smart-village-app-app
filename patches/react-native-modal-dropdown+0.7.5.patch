diff --git a/node_modules/react-native-modal-dropdown/components/ModalDropdown.js b/node_modules/react-native-modal-dropdown/components/ModalDropdown.js
index 1a10990..fb369cd 100644
--- a/node_modules/react-native-modal-dropdown/components/ModalDropdown.js
+++ b/node_modules/react-native-modal-dropdown/components/ModalDropdown.js
@@ -1,20 +1,19 @@
-import React, { Component } from 'react';
+import PropTypes from 'prop-types';
+import { Component } from 'react';
 import {
-  StyleSheet,
-  Dimensions,
-  View,
-  Text,
-  TouchableWithoutFeedback,
-  TouchableNativeFeedback,
-  TouchableOpacity,
-  TouchableHighlight,
-  Modal,
   ActivityIndicator,
+  Dimensions,
   FlatList,
+  Modal,
   Platform,
-  TextInput
+  StyleSheet,
+  Text,
+  TextInput,
+  TouchableHighlight,
+  TouchableOpacity,
+  TouchableWithoutFeedback,
+  View
 } from 'react-native';
-import PropTypes from 'prop-types';
 
 const TOUCHABLE_ELEMENTS = [
   'TouchableHighlight',
@@ -46,6 +45,7 @@ export default class ModalDropdown extends Component {
       PropTypes.array,
     ]),
     searchPlaceholder: PropTypes.string,
+    searchPlaceholderTextColor: PropTypes.string,
     style: PropTypes.oneOfType([
       PropTypes.number,
       PropTypes.object,
@@ -398,6 +398,7 @@ export default class ModalDropdown extends Component {
         }}
         value={searchValue}
         placeholder={searchPlaceholder}
+        placeholderTextColor={this.props.searchPlaceholderTextColor}
       />
     );
   };
@@ -434,13 +435,17 @@ export default class ModalDropdown extends Component {
     );
 
     const touchableProps = {
-      key,
       accessible,
       onPress: () => this._onRowPress(item, index, separators),
       ...renderRowProps
     };
 
-    return <RowTouchable {...touchableProps}>{row}</RowTouchable>;
+    // React warns when `key` is part of the spread props. Pass it directly instead.
+    return (
+      <RowTouchable key={key} {...touchableProps}>
+        {row}
+      </RowTouchable>
+    );
   };
 
   _onRowPress(rowData, rowID, highlightRow) {
