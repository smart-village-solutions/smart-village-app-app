// import PropTypes from 'prop-types';
// import React from 'react';
// import _filter from 'lodash/filter';
// import { SafeAreaView, ScrollView, View } from 'react-native';

// import {
//   Image,
//   BoldText,
//   Button,
//   TextList,
//   Title,
//   TitleContainer,
//   TitleShadow,
//   Touchable,
//   Wrapper,
//   WrapperWrap
// } from '../components';
// import { getQuery } from '../queries';

// export const PointsOfInterestCategories = ({ data }) => {

//   const { navigation } = this.props;
//   const { category } = data && ata[query] &&
//     _filter(data[query], (pointOfInterest) => isFiltered(category.name)).map(
//       (pointOfInterest) => {
//         return (category)
//         });




//   return (
//       <SafeAreaView>
//       <ScrollView>
//       {!!category && <RegularText>{category}</RegularText>}
//         <TextList navigation={navigation} data={} />
//       </ScrollView>
//     </SafeAreaView>

//   );
// };

// PointsOfInterestCategories.propTypes = {
//   data: PropTypes.object.isRequired
// };


