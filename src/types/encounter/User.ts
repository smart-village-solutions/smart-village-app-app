export type User = {
  birthDate: string;
  createdAt: string;
  firstName: string;
  imageUri: string;
  lastName: string;
  phone: string;
  userId: string;
  verified: boolean;
  village: string;
};
