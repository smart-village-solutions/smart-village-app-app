import { HtmlStackNavigator } from '../../src/navigation/stackNavigators';

describe('HtmlStackNavigator', () => {
  it('creates correctly', () => {
    expect(HtmlStackNavigator).toBeTruthy();
  });
});
