import { IndexStackNavigator } from '../../src/navigation/IndexStackNavigator';

describe('IndexStackNavigator', () => {
  it('creates correctly', () => {
    expect(IndexStackNavigator).toBeTruthy();
  });
});
