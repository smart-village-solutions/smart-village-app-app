import { HomeStackNavigator } from '../../src/navigation/HomeStackNavigator';

describe('HomeStackNavigator', () => {
  it('creates correctly', () => {
    expect(HomeStackNavigator).toBeTruthy();
  });
});
