import { AppDrawerNavigator } from '../../src/navigation/AppDrawerNavigator';

describe('AppDrawerNavigator', () => {
  it('creates correctly', () => {
    expect(AppDrawerNavigator).toBeTruthy();
  });
});
