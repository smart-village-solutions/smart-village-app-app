import { StaticStackNavigator } from '../../src/navigation/StaticStackNavigator';

describe('StaticStackNavigator', () => {
  it('creates correctly', () => {
    expect(StaticStackNavigator).toBeTruthy();
  });
});
